services:
  backend:
    container_name: "research-infographic-backend-${RUN_ID:-default}"
    build:
      context: ./backend
    ports:
      - '${BACKEND_PORT:-8000}:8000'
    env_file:
      - .env
    command: uvicorn app.main:app --host 0.0.0.0 --port ${BACKEND_PORT:-8000}
  frontend:
    container_name: "research-infographic-frontend-${RUN_ID:-default}"
    build:
      context: ./frontend
    ports:
      - '${FRONTEND_PORT:-3000}:3000'
    env_file:
      - .env
    depends_on:
      - backend
    command: npm run dev -- --hostname 0.0.0.0 --port ${FRONTEND_PORT:-3000}
